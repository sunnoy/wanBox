name: build-test
on:
  push:
    branches:
    - main
jobs:
  msvc:
    name: 'openvpn-build'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout openvpn-build
        uses: actions/checkout@v3
      - name: check
        run: |
          ls -lh /
